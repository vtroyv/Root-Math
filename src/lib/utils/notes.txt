1) Now that we have a globalStore via zustand, so now bascially for each taskType for all interactions we need to constantly update the globalState 
for the lesson via the taskState key

2) When the user clicks submit we then read from that global taskState to see the current state of the task using the activeIndex in the handlesubmit function 
in the LessonMathDisplay component

3) Now we must update the handleSubmit function to provide correct/incorrect aswell as accurate feedback for each task 







-----------------------------------------------------------------------------------------------------------------------------------------------------------
##THE QUESTIONS SECTION ##

-> essentially i need to rebuild the userProgress functionality with the questions, to enable feedback to be stored uniquely for each student, 
and when a student attempts a problem for the first time that userprogress for the question should be created in the DB or fetched on all subsequent requests 

1) Start by recreating the dynamicdata api hook from redux for the problem sheets, and all the functionality e.g. mongoDB stuff for it 

